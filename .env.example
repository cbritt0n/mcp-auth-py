# mcp-auth-py Configuration Example
# Copy this to .env and configure your settings

# JWT Settings (for local provider)
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_ALGORITHM=HS256

# Auth Provider Selection
# Options: local, google, aws, azure
AUTH_PROVIDER=local

# Redis JWKS Caching (optional - improves performance)
REDIS_JWKS=false
REDIS_URL=redis://localhost:6379/0

# Provider-specific configurations (set as JSON strings)

# Google OAuth2 Configuration
# PROVIDER_CONFIG={"audience": "your-google-client-id.apps.googleusercontent.com"}

# AWS Cognito Configuration
# PROVIDER_CONFIG={"cognito_region": "us-west-2", "cognito_user_pool_id": "us-west-2_XXXXXXXXX", "audience": "your-app-client-id"}

# Azure AD Configuration
# PROVIDER_CONFIG={"tenant": "your-tenant-id", "audience": "your-app-client-id"}

# Development/Testing
# For local development, you can use the default JWT_SECRET
# Generate test tokens with: mcp-auth-generate-token --help
